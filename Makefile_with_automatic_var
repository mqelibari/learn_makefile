foo.o: foo.c
	gcc -c $^ -o $@



# the automatic Varibale $^ will replace the varibale to directory the file is found + filename

# the autmatic Variable $@ replaces the varibale to the name of the target

# the automatic Varibale $< is replaced by the name of the first prerequisite

# the automatic Varibale $? is replaced by only those files that have changed
